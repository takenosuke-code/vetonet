pydantic>=2.0
requests>=2.28.0
flask>=3.0.0
flask-cors>=4.0.0
PyJWT>=2.8.0
groq>=0.4.0
anthropic>=0.40.0
python-dotenv>=1.0.0
gunicorn>=21.0.0
psycopg2-binary>=2.9.0
supabase>=2.0.0

# ML classifier dependencies (optional - graceful fallback if not installed)
sentence-transformers>=2.2.0
scikit-learn>=1.3.0
joblib>=1.3.0

# Protocol integrations (optional)
# mcp[cli]>=1.0.0  # MCP server for Claude/GPT agents
httpx>=0.27.0  # For World ID and x402 integrations
